# server

Django 6 backend for the `book-cv` website.

- `/admin/` for content and user administration
- `/api/health/` for container checks
- `/api/profile/` and `/api/projects/` for future frontend integration
